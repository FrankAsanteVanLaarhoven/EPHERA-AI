# README media assets

Assets used by the root [`README.md`](../../README.md).

| Path | Use |
| --- | --- |
| `brand/` | Logo, app icon, horizontal lockup for documentation headers |
| `screenshots/` | Product UI captures (splash, welcome, home, listening, voice, services, design board) |

Source masters live under `apps/mobile/assets/` and `packages/brand/`. Prefer regenerating from masters rather than editing these copies.
