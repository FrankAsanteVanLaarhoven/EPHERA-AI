# Local development only — never commit real secrets

POSTGRES_USER=ephera
POSTGRES_PASSWORD=ephera_dev_only
POSTGRES_HOST=localhost
POSTGRES_PORT=5433

LEDGER_DATABASE_URL=postgres://ephera:ephera_dev_only@localhost:5433/ephera_ledger
IDENTITY_DATABASE_URL=postgres://ephera:ephera_dev_only@localhost:5433/ephera_identity
OPERATIONS_DATABASE_URL=postgres://ephera:ephera_dev_only@localhost:5433/ephera_operations

REDIS_URL=redis://localhost:6379
NATS_URL=nats://localhost:4222
TEMPORAL_ADDRESS=localhost:7233
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=ephera
MINIO_SECRET_KEY=ephera_dev_only
MINIO_BUCKET=ephera-evidence

# Cognito is mocked locally
COGNITO_REGION=eu-west-1
COGNITO_USER_POOL_ID=local
COGNITO_CLIENT_ID=local

# Never put payment-provider secrets or HSM material in mobile apps
EPHERA_ENV=local
